# Antigravity

Antigravity keeps its existing settings, keybindings, extensions, and Godot
workflow. The shared MCP file is generated and linked separately. Configure
the provider endpoint through the client environment as
`http://127.0.0.1:20128/v1`; do not add a direct provider endpoint.
