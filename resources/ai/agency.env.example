# Copy to ~/.config/9router/agency.env, or run:
#   ./scripts/init-agency.sh --set-api-key
#
# This file must never be committed. The vault script includes this path in
# the encrypted `all` scope so a restored machine can reuse the local setup.
NINEROUTER_API_KEY=
